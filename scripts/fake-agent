#!/usr/bin/env python3
from __future__ import annotations

import sys


prompt = sys.stdin.read()

if "DECISION: approve or reject" in prompt:
    print(
        """DECISION: approve
COMMENT: Fake verifier approved the document.
BODY:
# Review

The fake verifier accepts this document for offline workflow testing.
"""
    )
else:
    print(
        """# Fake Exploration Result

::: {.theorem title="Fake theorem"}
For every $n$, this fake document exercises the autoprover workflow.
:::

::: {.proof}
This is a deterministic fake proof used for local development.
:::
"""
    )
