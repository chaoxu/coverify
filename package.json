{
  "name": "coverify",
  "version": "2.0.0",
  "description": "Math proof-search agent: the math-proof-search campaign contract compiled onto pi",
  "type": "module",
  "private": true,
  "bin": {
    "coverify": "src/cli.ts"
  },
  "scripts": {
    "coverify": "bun run src/cli.ts",
    "check": "tsc --noEmit && bun run scripts/conformance-check.ts"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.83.0",
    "@earendil-works/pi-ai": "^0.83.0",
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@earendil-works/pi-tui": "^0.83.0",
    "pi-claude-bridge": "^0.6.3",
    "typebox": "^1.3.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
