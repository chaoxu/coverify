[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autoprover"
version = "0.1.0"
description = "CLI-first Cosheaf tool harness for mathematical proof runs"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
autoprover = "autoprover.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
