interface:
  display_name: "Coverify Context"
  short_description: "Build compact Coverify task context"
  default_prompt: "Use $coverify-context-builder to build concise task context from the relevant Cosheaf pages, issues, PRs, reviews, tried routes, and artifacts."
